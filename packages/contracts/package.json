{
  "name": "@meallink/contracts",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "typecheck": "tsc --noEmit",
    "test": "hardhat test",
    "clean": "hardhat clean",
    "node": "hardhat node",
    "deploy:local": "hardhat run scripts/deploy-local.ts --network localhost",
    "seed:local": "hardhat run scripts/seed-local.ts --network localhost",
    "e2e:local": "hardhat run scripts/e2e-local.ts --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy-sepolia.ts --network sepolia",
    "seed:sepolia": "hardhat run scripts/seed-sepolia.ts --network sepolia",
    "verify:sepolia": "hardhat run scripts/verify-sepolia.ts --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-mocha-ethers": "^3.0.7",
    "@nomicfoundation/hardhat-verify": "^3.0.22",
    "@openzeppelin/contracts": "^5.6.1",
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.10.0",
    "hardhat": "^3.12.0",
    "typescript": "^5.7.2"
  }
}
